package lms.demo.model;

  public enum BorrowStatus {
    BORROWED,    // Stored as "BORROWED" in database when using EnumType.STRING
    RETURNED     // Stored as "RETURNED" in database when using EnumType.STRING
  }

